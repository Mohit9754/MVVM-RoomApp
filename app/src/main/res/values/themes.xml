<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ApiWithMVVM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="colorControlActivated">@color/black</item> <!-- Active indicator color -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="backgroundColor">@color/white</item>
        <item name="android:windowBackground">@color/white</item> <!-- Use this for setting background color -->

        <item name="android:overScrollMode">never</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/hint_text_color</item>
        <item name="android:autofilledHighlight" tools:targetApi="o">@android:color/transparent</item>

        <!--        <item name="android:hintTextColor">@color/red</item>-->

        <!--        <item name="android:hintTextColor">@color/te</item> &lt;!&ndash; Hint text color &ndash;&gt;-->


        <!-- Optionally set color for specific text types -->
        <item name="android:textColorPrimary">@color/black</item>  <!-- For primary text -->
        <item name="android:textColorSecondary">@color/black</item>
    </style>

    <style name="Theme.ApiWithMVVM" parent="Base.Theme.ApiWithMVVM" />

    <style name="CustomTabTextAppearance" parent="TextAppearance.Design.Tab">
        <!-- Capitalize the text -->
        <item name="textAllCaps">false</item>
        <!-- Optionally adjust other text properties -->
        <item name="android:textSize">14sp</item>

        <!--        <item name="android:textColor">@color/your_text_color</item>-->
    </style>
</resources>